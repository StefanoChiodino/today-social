<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <%= label f, :yesterday %>
  <%= textarea f, :yesterday %>
  <%= error_tag f, :yesterday %>

  <%= label f, :today %>
  <%= textarea f, :today %>
  <%= error_tag f, :today %>

  <div>
    <%= submit "Save" %>
  </div>
<% end %>
